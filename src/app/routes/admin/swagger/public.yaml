 tags:
      -   name : course
          description : course APIs
      -   name : category
          description : category APIs
      -   name : blog
          description : blog APIs
      -   name : product
          description : product APIs

#  components:
#   schmeas:
#     securitySchemes:
#       bearerAuth:
#         type : http
#           scheme : bearer
#               bearerFormat: JWT
#  components:
#       schemas:
#           securitySchemes:
#               bearerAuth:
#                   type : http
#                   scheme : bearer
#                   bearerFormat: JWT
